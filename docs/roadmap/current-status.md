# Sachima Roadmap Current Status

> Lean project dashboard for AGENT-facing project state. This file aligns the active product goal, stage, task progress, blockers, non-approvals, and next allowed work. It is not a repository-process ledger, results database, or historical diary.

## How to read this file

- Use this page to decide what the project is trying to achieve now, what is done, what is blocked, and what can be requested next.
- Task rows record work-state only: `Done`, `In progress`, `Blocked`, `Not started`, or `Paused`.
- A `Done` row is a project-task state, not a quality certificate. Quality-control evidence remains outside the task-state row unless it changes the task state.
- Do not record repository/process bookkeeping, external-check matrices, hashes, or流水账 here.
- Long design details live in the referenced plans/runbooks. Runtime truth still requires fresh checks before any live or production-facing action.

## Dynamic status policy

Machine-owned dynamic status is intentionally absent from this lean dashboard. This page records only the human/developer-facing task state and boundary posture.

## Current project position

| Field | Current truth |
|---|---|
| Product goal | Production-grade AI workbench inside a custom IM channel, guided by the private-Hermes runtime spine: one per-user Hermes Agent, one `task_id` spine, supervised external local AGENT event streams, optional Temporal durability, and controlled delivery surfaces. |
| Active mainlines | (1) Preserve the Private Hermes Runtime Spine as the highest architecture guidance for Hermes, Claude Code, Codex CLI, and engineers. (2) Keep the R0-R5 runtime-spine foundation done as local/offline/default-off slices under `docs/plans/2026-07-03-sachima-private-hermes-runtime-spine-development-plan.md`. (3) Treat the already-completed `agent-run-supervisor` foundation as support for the live-stream consumption mainline: adapter, host-local read-only smoke shape, persistent lifecycle, workbench view, production-shaped E2E, and safe live-progress projection. (4) Advance live-stream consumption through `docs/plans/2026-07-05-ars-live-stream-integration-plan.md`: LS1 workbench composition (done), LS2 source binding (done), LS3 caller-API compatibility smoke (done), and LS4-A default-off local/offline query activation gate (done), then request a separately approved LS4 runtime/Gateway query surface. (5) Keep P7 downstream real-send canary execution paused as delivery-safety support, not the mainline. |
| Current stage | R0 planning reset, R1 runtime-spine core, R2 supervisor execution port, R3 Temporal attach-only durable bridge, R4 workspace + permission + projection integration, and R5 controlled canary / product hardening are done as local/offline/default-off foundation. The runtime-spine architecture is saved under `docs/architecture/`, the active R0-R5 development roadmap is saved under `docs/plans/`, and old phase plans are registered as superseded active roadmap / retained support foundation. The `agent-run-supervisor` live-progress safe projection exists as a standalone local/offline read-model; LS1 has wired that projection into the combined task workbench view, LS2 has added private source binding / cursor state for tracked sessions, LS3 has locked the synthetic-artifact caller-API compatibility smoke as a local/offline task, and LS4-A has added a default-off local/offline `query_task_live_progress` activation gate over that binding + combined view. Separately, the approved semantic-delegation control/lifecycle/result path is active in the resident Gateway; its rich status-card follow-up is a docs-only plan whose source implementation has not started. The LS4 runtime/Gateway/Feishu query surface remains a separately approved gate. |
| Current completed foundation | R1 runtime-spine core, R2 supervisor execution port, R3 Temporal attach-only durable bridge, R4 workspace + permission + projection integration, R5 controlled canary / product hardening, agent-run-supervisor adapter/persistent lifecycle/workbench/E2E foundations, agent-run-supervisor live-progress safe projection, LS1 live workbench composition, LS2 live-progress source binding, LS3 caller-API compatibility smoke, LS4-A default-off query activation gate, S1 integration design, S2 local/offline adapter seam, S3 Activity/controller design, S3 hermetic-local Activity implementation, S4 read-only real-agent step design, S4 read-only real-agent step implementation, S5 downstream delivery reconnect design, S5 downstream delivery reconnect implementation, P7 bounded real-send canary request-packet preparation, semantic AGENT delegation, native delegation lifecycle/result projection, and bounded same-Session continuation evidence are complete. The native delegation status card remains planning-only. |
| Current design authority | `docs/architecture/private-hermes-runtime-spine-design.md` and `docs/architecture/private-hermes-runtime-spine-architecture.svg` are the highest architecture guidance. `docs/plans/2026-07-03-sachima-private-hermes-runtime-spine-development-plan.md` is the active development roadmap under that architecture, and `docs/roadmap/superseded-plans.md` records old roadmap supersession. They supersede phase-label/topology interpretations that imply four independent lanes, Gateway-owned runtime lifecycle, Temporal-owned AGENT processes, or heavy Admission/Delivery/FlowWeaver as P0 components. |
| Current boundary | R1/R2/R3/R4/R5 runtime-spine work and the existing live-stream foundation remain local/offline/default-off. The approved semantic-delegation control/lifecycle/result surface is the explicit resident-runtime exception and uses the active ARSD Socket path. Outside that exception, this status page does not approve additional live/default-on behavior, the new status card, unrelated delivery, production config, Gateway lifecycle, Temporal Worker/service startup, public webhook exposure, real external Sachima ingress, new execution presets, or ad-hoc direct AGENT/ACP execution. |

## Stage / feature board

| Stage / task | Status | Work-state note | Role in the mainline |
|---|---|---|---|
| Mainline calibration | Done | Current core direction is the Private Hermes Runtime Spine: one per-user Hermes Agent, one `task_id` spine, `needs_agent` / `needs_durable` mode flags, and deferred heavy layers. | Reclassified earlier P5/P6/P7 work as support foundation rather than wasted work. |
| Private Hermes Runtime Spine architecture | Done | Claude Code Architect authored the final design and SVG after Hermes/Codex roundtable convergence. | Highest guiding architecture for Hermes, Claude Code, Codex CLI, and engineers. |
| R0 runtime-spine planning reset | Done | New R0-R5 development plan and superseded-plans register define the active roadmap. | Makes old S1-S5/P5/P6/P7 plans support foundation rather than active roadmap. |
| R1 runtime-spine core | Done | Local/offline core implementation task complete. | Provides Task Registry, refs-only Event Log, monotonic seq authority, deterministic Status Projection, static Capability Registry, and typed LaunchSpec for the next candidate gate. |
| R2 supervisor execution port | Done | Deterministic fake/offline execution-port implementation task complete. | Adds offline/fake supervisor port semantics only. |
| R3 Temporal attach-only durable bridge | Done | Local/offline attach-only durable bridge implementation task complete. | Adds attach-only durable bridge contracts without Worker/service startup. |
| R4 workspace + permission + projection integration | Done | Local/offline implementation task complete: workspace single-writer lease, permission roundtrip, read-only/default-deny role policy, and platform-neutral refs-only view model. | Adds lease, permission roundtrip, and platform-neutral view model. |
| R5 controlled canary / product hardening | Done | Default-off local/offline preparation task complete: controlled canary packet, dry-run observability report, stable stop/rollback semantics, and safe-label receiver-bridge boundary. | Keeps canary/product hardening default-off; real send remains separate. |
| AgentRunSupervisorPort adapter | Done | Local/offline implementation task complete: injected/default-fake backend adapter only, no real process startup. | Adds a Sachima-side adapter from runtime-spine `ExecutionPort` semantics to an injected supervisor backend surface. |
| Host-local read-only real AGENT smoke | Done | Local/offline implementation task complete: bounded read-only smoke harness over the adapter plus fail-closed real-supervisor readiness gate; no real launch by default and no runner is marked ready without a local path whose bytes match the pinned digest. | Proves the first host-local read-only supervisor-smoke shape while preserving default-off/non-live boundaries. |
| Persistent session lifecycle | Done | Local/offline implementation task complete: persistent create/attach re-attach over a reconstructed port (no duplicate `agent_attached`; fresh/missing backend fails closed, no respawn), `stream(since_seq=...)` resume cursor + refs-only `LifecycleSnapshot` safe resume data, adapter-local default-off `close` handoff, explicit status/liveness backend-read-failure policy (transient stable code, no mark/kill/mutation), and no role/workspace-ref drift on re-attach. | Hardens lifecycle and lease/task binding. |
| StatusProjection + task workbench view | Done | Local/offline implementation task complete: a refs-only workbench view composing the `TaskViewModel` Status Projection with the `LifecycleSnapshot` for a locally tracked session — deterministic, byte-stable, fail-closed, no-leak — plus a focused test file, a runbook, and package exports. No live UI/IM/delivery. | Makes supervised task state visible through safe refs-only fields. |
| Single-user production-shaped E2E | Done | Local/offline implementation task complete: a default-off single-user fixture that drives the adapter through reconstruct/attach (one session, one `agent_attached`), an optional deterministic permission-wait + operator-decision path, a renderable workbench view before cleanup, deterministic kill/orphan-free cleanup + lease release, and a rollback-safe restart, summarized in a fail-closed, byte-stable, no-leak `ProductionE2EReport`; plus a focused test file, a runbook, and package exports. This does **not** mean production上线, live ingress, real delivery, Gateway behavior, Temporal Worker startup, or production config. | End-to-end proof while preserving non-approvals. |
| Live stream safe projection | Done | Local/offline projection task complete: `LiveProgressProjection` maps `agent-run-supervisor` progress artifacts and cursor pages into refs-only, fail-closed, byte-stable Sachima projection data. It is not yet wired into the task workbench/query chain. | Provides the safe read-model foundation for live-stream consumption. |
| Live stream workbench/query integration plan | Done as docs-only plan | `docs/plans/2026-07-05-ars-live-stream-integration-plan.md` defines LS1 through LS4: workbench composition, source binding, caller-API compatibility smoke, and later runtime/query activation. Source implementation remains unapproved until a named LS approval. | Governs the next live-stream consumption mainline. |
| LS1 live workbench composition | Done | Local/offline slice complete: composes the `AgentRunSupervisorWorkbenchView` with the `LiveProgressProjection` into one refs-only, fail-closed, byte-stable combined workbench view for a locally tracked session. No live UI/IM/delivery. | First live-stream consumption slice under the integration plan. |
| LS2 live-progress source binding | Done | Local/offline slice complete: a host-owned `LiveProgressSourceBindings` layer resolves a tracked session to its private reader-only `artifact_dir`, safe `artifact_ref`, and foreign `last_seen_cursor`, and drives the LS1 view from that binding. The private path is never serialized/logged; the foreign cursor never enters `TaskEventLog`. | Binds tracked sessions to their private live-progress source. |
| LS3 caller-API compatibility smoke | Done | Local/offline slice complete: a read-only smoke reads synthetic `progress.json` + `normalized-events.jsonl` through the real `agent_run_supervisor.hermes_caller.events` caller API and maps them into the existing refs-only projection / combined workbench view plus a stable outcome report, locking Sachima's reader contract, `has_more`/`next_cursor`/`after_seq` cursor behavior, nullable-field normalization, legacy no-`seq` fallback, and corrupt / import-absent fail-closed boundaries against the current ARS API. Reads fixtures only — no real AGENT/acpx/npx launch, no Gateway/Feishu/live/default-on, and no hard import of `agent-run-supervisor` at module load. The real caller API now comes only from the exact-pinned `agent-run-supervisor` PyPI extra (installed in CI via `dev`), so real-API tests run for real there and skip only on lean environments without the extra. | Pins field/cursor/nullable compatibility so later runtime wiring does not guess field names. |
| LS4-A default-off query activation gate | Done | Local/offline slice complete: a `query_task_live_progress(task_id, session_id, after_seq=None, limit=100)` entrypoint (plus a `LiveProgressQueryService`) that is **disabled by default** — a caller must present an explicit `LiveProgressQueryActivationGate` for an approved `local_offline`/`hermes_internal` surface before any read. A disabled query fails closed with a stable code and never calls the reader, appends no `TaskEventLog` event, updates no binding cursor, and launches nothing. An enabled local/offline query resolves the LS2 binding and composes the LS1 combined view using the private `artifact_dir` for the reader only (never serialized) and `after_seq` override else the binding cursor; it stays read-only. Gateway/Feishu/IM/delivery/public-ingress/Temporal-Worker/production/default-on surfaces are denied by an exact allowlist. | Gives Sachima a controlled, default-off local/offline live-progress query without touching runtime/Gateway. |
| Packaged agent-run-supervisor dependency | Done | Dependency task complete: `agent-run-supervisor==0.7.8` is declared as an exact-pinned optional extra (mirrored into `dev` for CI), the pin checker expects `0.7.8`, and every source-path channel — the gateway `sys.path` shim envs, the test sibling-checkout constant, and runbook `PYTHONPATH` instructions — is retired repo-wide with a packaging guard test (`test_no_agent_run_supervisor_source_path_references`) plus pin-consistency and lock-resolution tests. Unreleased ARS changes are validated only by installing a locally built wheel/editable package into an isolated environment; the checker reports such versions honestly. All lazy-import, fail-closed, and default-off gates are unchanged. | Makes the installed exact-pinned distribution the only way Sachima reaches agent-run-supervisor. |
| ARS-INT formal library backend / turn dispatcher seam | Done | Local/offline slice complete: a default-off `AgentRunSupervisorLibraryConfig` gate (explicit `enabled=True` + `approval_ref`, read-only persistent roles only, pinned local non-launcher acpx, private paths repr-excluded with no serialization surface) plus an `AgentRunSupervisorLibraryBackend` implementing the existing injected backend protocol over in-process `agent_run_supervisor` library calls — lazy facade imports only, no CLI shellout, status/liveness synthesized from local record + lease + latest-turn observation (`no_op` is failure; closed-without-terminal-turn is honest `ambiguous`; phase-A `signal` fail-closed; `kill` = abort+close→`cancelled`) — an `AgentRunSupervisorTurnDispatcher` (refs-only single-flight goal/prompt turns; goal payloads compiled through ARS `GoalSpec`/`compile_goal_prompt`, never a literal `/goal`; automatic per-turn `LiveProgressSourceBindings` rebind with cursor reset; payload text and private turn dirs never leak into outcomes/events/serialization), a one-call `bind_agent_run_supervisor_execution` bundle feeding the LS4-A-gated display chain, and a gateway `SACHIMA_LIVE_PROGRESS_BACKEND=library` mode (double default-off behind the `hermes_internal` surface plus an explicit private config file; `fake` + static bindings file stays the default). No real AGENT/acpx/npx launch, no live/default-on, no Gateway restart. | Replaces controller-script/CLI glue with the formal typed library seam the runtime spine consumes directly. |
| ARS 0.6.3 socket D0/D1 baseline | Done | Local/offline slices merged: the exact pin moved to `agent-run-supervisor==0.6.3`, and a pure offline `arsd` Socket contract module (default-off config, injected client facade, request builder, exact response validators, stable error mapping) landed with its contract tests. Nothing is wired into Runtime Spine or Gateway. | Provides the offline Socket contract foundation the Socket API v3 work builds on. |
| ARS 0.7.1 Socket API v3 Spec + implementation plan | Done; historical/superseded | The docs-only pair that first defined the integration contract (closed seven-operation set, Session no-close + Run-terminal model, private run+session binding, transport-neutral turn result, foreign-cursor and no-leak rules, stable failure categories, legacy `library` retirement, offline acceptance matrix) and the five-stage slicing/evidence model. On explicit acceptance of the 0.7.6 calibration below it became **historical/superseded for this integration** and no longer holds governing authority; it is retained as the accepted baseline the calibration was measured against. | Provenance for the contract now carried by the accepted 0.7.6 pair. |
| ARS 0.7.6 Socket API v3 Spec + implementation plan calibration | Accepted; **active governing integration authority** | Accepted docs pair at `docs/plans/2026-08-17-ars-0.7.6-socket-api-v3-integration-spec.md` and `docs/plans/2026-08-17-ars-0.7.6-socket-api-v3-integration-plan.md`, targeting released ARS 0.7.6. Wire, schema, operation set, Session/Run model, terminal vocabulary, and error codes are unchanged from 0.7.1; the calibration carries the behavior deltas — turn-timeout constants, ARS-internal evidence handling, profile/ACP-mode derivation, the sixth `server_info.limits` key `max_run_event_budget_bytes` with its admission arithmetic, and 0.7.6 permission mediation — plus explicit per-Run `requested_model`/`requested_effort` obligations. Scope, the strict P1→P5 ordering, and every prior non-approval are unchanged; no stage was added. On acceptance it supersedes the 0.7.1 pair above as the governing Socket integration authority. Accepting the documents settles the contract and the slicing/evidence model only; every standing non-approval remains in force — no commit/push/PR/merge, no deploy/restart/reload, no live enablement of `arsd`, no interaction with a real Sachima or `arsd` daemon, and no real AGENT/ACP execution. | The governing contract for this integration, executable against the released ARS version. |
| ARS 0.7.6 Socket API v3 source implementation | P1–P5 done | All 5 stages. Source-implementation approval covers the **complete P1→P5 plan**, not P1 alone; the strict one-stage-in-flight ordering still governs, so P2 begins only once P1's exit criteria hold. Approval to implement lifts none of the standing non-approvals: no commit/push/PR/merge, no deploy/restart/reload, no live enablement of `arsd`, no interaction with a real Sachima or `arsd` daemon, no real AGENT/ACP execution, and no write-capable grant. **P1 — protocol truth:** the exact pin moved to `agent-run-supervisor==0.7.6` (extra + `dev` mirror + regenerated `uv.lock` + `EXPECTED_AGENT_RUN_SUPERVISOR_VERSION` + a narrowed `[tool.uv].exclude-newer-package` date), and the offline contract module now tells the truth about Socket API v3: `api_version`/`supported_api_versions`/`schema_version` at `3`, the closed seven-operation set (no `session_close`), the exact five-key `server_info` result with its **six**-key `limits` block, the three-key submit ack with a private `session_id`, create-by-omission / existing-only-reuse Session construction (a present-null `session_id` can never be built), the dotless Session-id grammar, `run_status` / `run_cancel` / `session_status` / `session_list` validators, the facade's four added read operations, mirrored `RunLimits` defaults and `LIMIT_*` maxima (`turn_timeout_seconds` `21600.0`, inclusive maximum `604800.0`), `STRUCTURAL_MAX_RUN_EVENT_BUDGET_BYTES` mirrored as a maximum, the admission-product pre-check against the negotiated budget, `cwd`-inside-`workspace_root` containment, and a private `binding_ledger_path` for P2 to consume. Every mirror is drift-locked against the installed distribution and runs for real (no skips); the three removed-module test-fallout sites (`import_compat`, the library-backend real-library section, the caller-API import assertion) are retired outright with no compatibility shim. `fake` is untouched and green, `arsd` stays unwired and default-off, no daemon/socket/Gateway interaction occurred, and the grant allowlist stays exactly `{read, search}`. **P2 — durable private binding ledger:** a new `arsd_run_binding_ledger` module gives the `(task_id, session_id, dispatch_ref)` -> `(run_id, ars_session_id)` binding a restart-safe home. The key is the full triple — exactly `derive_arsd_request_id`'s inputs, so the same `dispatch_ref` under two spine sessions binds two records instead of aliasing onto one — and the stored `request_id` is a derived witness that fails closed on read when it disagrees with its own key. One record per dispatch moves through two states: `begin_pending()` writes the submission intent (with its `payload_digest` and `resolver_refs`) and lands it on disk before any submit exists, and `finalize_accepted()` promotes that same record in place; a finalize with no intent, a second finalize with different material, a same-key rewrite, or a replaced ARS Session each fail closed on the module's own `runtime_arsd_binding_conflict`, and a record left `pending` stays unresolved — never auto-finalized, never deleted, never replayed. Writes are serialize -> temp sibling -> `os.replace`, and a torn, empty, or unparseable file is `runtime_invalid_arsd_binding` with the damaged bytes left in place, never silently reset. The file sits on the private host path P1 validated, outside the tracked repo; raw `run_id` / `ars_session_id` stay off every repr and out of `as_dict()` behind a `run_<digest8>` ref, both record states are swept for prompt/credential/socket-path/remote text, and nothing here touches the `TaskEventLog`. Still no backend, no socket call, and no daemon contact: P2 stores identity and starts nothing. **P3 — neutral turn seam and `turn_dir` removal:** a new `supervisor_turn_backend` module owns one Sachima-owned validated turn contract, and the library-private turn directory is gone from the tree — proven, not asserted, by a bare-identifier guard over `sachima_supervisor/`, `gateway/`, and `scripts/` that goes from nine hits to zero (the E2 smoke script's `turn_dirs` plural is deliberately out of scope: it is a removed-module defect owned by P5, not a `turn_dir` hit). `SupervisorTurnResult` carries exactly five safe fields — `run_ref`, `source_kind`, `source_ref`, `supervisor_status`, `foreign_cursor` — with no path and no raw id on any repr or serialized surface; the private locator rides only on the `DispatchedSupervisorTurn` handoff, which opts out of `repr` and offers no serialize surface at all. The concrete `LibraryTurnResult` type is deleted, and both concrete-type couplings are replaced by an exact-type factory allowlist: neither `AgentRunSupervisorTurnDispatcher.__init__` nor `AgentRunSupervisorExecutionBinding.__post_init__` names a backend class, and a protocol-shaped duck type — one that really does satisfy the `SupervisorTurnBackend` protocol — and a hostile subclass are both refused at the composition root. The library's turn vocabulary reaches callers only through a closed collapse into the transport-neutral set, so a backend-private token such as `no_op` can no longer appear on a dispatch outcome. Source bindings are tagged: `LiveProgressSource` gains a serialized `source_kind`, `bind_source()` generalizes `bind()` over a tagged private locator (`artifact_file` -> `artifact_dir`, `arsd_run` -> `run_id`), and the serializable projection stays refs-only — no `artifact_dir`, no `run_id`, no `socket_path`. The real-execution activity's artifact-count read off the removed runtime outcome is deleted rather than renamed. Still no socket call, no `arsd` backend, and no daemon contact: P3 changes shape, not reach. **P4 — the `arsd` backend:** a new `arsd_supervisor_backend` module submits once, observes honestly, cancels precisely, and never fabricates an outcome — default-off, doubles-only, and composed by nothing. Constructing it is the gate (an `enabled=True` config or it fails closed before any operation), and composition negotiates `server_info` once so a composed backend has proven it is talking to the reviewed contract. `run_turn` renegotiates `server_info` afresh immediately before **every new** admission and pre-checks that admission's frozen `max_event_bytes * max_events` product against **that** negotiation's budget — two sequential admissions negotiate twice, and a second negotiation returning a lower budget refuses the second request before a submit is spent. The pending submission intent is written and durably landed before the first socket submit, and the validated three-key ack finalizes that same record in place with both `run_id` and `ars_session_id`: one logical record per dispatch, never two. The first turn creates a Session by omitting `session_id` and later turns pass the recorded id verbatim after a `session_status` reuse check that refuses a quarantined Session rather than auto-healing or recreating one; every Run — including every reuse turn — re-resolves its own `(requested_model, requested_effort)` pair from the policy maps, and a Session's `last_effective_*` observations feed nothing. An indeterminate submit, including a genuinely lost ack, maps to `runtime_arsd_submission_indeterminate` and leaves the intent fail-closed: no automatic replay, no new `request_id`, and the task's next turn is blocked until the separately invoked recovery entry point resolves it. That entry point rebuilds the payload through the same resolver from the intent's refs, fails closed with `runtime_arsd_binding_conflict` and **zero** socket calls when the rebuilt digest differs, and otherwise resends the identical `request_id` with the byte-equivalent frozen payload — no negotiation, no current-budget pre-check, never re-tuned to a changed budget — finalizing that same intent exactly once. Observation applies the R-6 three-hop mapping with all five terminals proven independently (`timed_out` and `unknown` reach canonical `failed` through different session states), while untrusted terminal evidence, a disconnect, an unproven cancel, and a `PERMISSION_VIOLATION` terminal each stay distinct on their own fixtures: a permission-violation terminal is a trusted failure that leaves the Session reusable and un-quarantined with no retry, a reported `CONFIG_FIDELITY` terminal is an ordinary trusted failure claiming no agent work, `kill` issues exactly `run_cancel` and never a Session operation, `signal` stays fail-closed, and a disconnect mutates nothing. The read model is an `ArsdLiveProgressReader` over `run_events` only, whose foreign cursor never becomes a canonical event seq, and the offline reader is now Sachima-owned and stdlib-only — the removed caller-API import is gone from `live_progress_projection.py`, so the `fake` path carries no `agent_run_supervisor` reference at all and the previously-skipped real-reader chain test runs for real. No test asserts an effective-configuration read-back (that stays gate E-9/E-10), and no surface presents Sachima's own request literal as a Run's effective configuration. `arsd` is admissible to the factory allowlist and exported from the package, and is still wired into no composition root: zero socket, daemon, network, or real-AGENT interaction anywhere in the stage. **P5 — legacy retirement, composition, and the default-off proof:** every seam that still reached a removed `agent_run_supervisor` module is retired, and `library` is now one fail-closed migration seam. A single distinct stable code, `runtime_library_backend_retired`, answers every way of selecting it — the backend env value or a library-shaped config file — with a fixed non-echoing message naming the two supported choices, `fake` and `arsd`; nothing is bound, dispatched, launched, rewritten, or fallen back to on the way to saying so, and the code is deliberately neither the daemon-unavailable code nor a generic invalid-config one. The library backend class, its config, its facade, its degraded session-inspection emulation, and the library-to-neutral status collapse are deleted rather than deprecated, so no half-working path survives; the offline caller seams follow (`local_offline`'s spec/invoker resolutions and the real-execution bridge's default runtime backend and goal composition all fail closed and must be injected), and the Phase E-2 persistent-session smoke script is deleted with the runtime it drove. **B-10** proves the retirement is total rather than partial: a guard over `sachima_supervisor/`, `gateway/`, `scripts/`, and `tools/` — comments and docstrings included — finds no reference to `role`, `workspace`, `session_runtime`, `session_inspect`, `goal`, `caller`, or `hermes_caller` as an `agent_run_supervisor` submodule anywhere outside the migration seam, and its own non-vacuity is checked. The composition root now offers exactly `fake` or `arsd`: unset or blank stays `fake`, `arsd` needs the exact `hermes_internal` surface gate AND an explicit private config file AND `enabled` exactly `True`, and there is no automatic fallback between backends in either direction. Composing the display service submits no Run — a composed bundle's dispatcher stays fail-closed until an internal caller injects a payload resolver — and the default `fake` path is proven in a fresh interpreter to import no `agent_run_supervisor` at all. With the library gone the neutral protocol's `dispatch_ref` is mandatory, and the dispatcher supplies the request's own claim-check ref so a re-dispatch resolves against the same durable record instead of minting a second. P5 also closes the active-Run exclusion P4 left open (Spec §7.4): before a second dispatch may write an intent or submit, this task's recorded Run must be proven terminal — an accepted Run with no terminal blocks it, and so does an observation that failed or could not be trusted, because uncertainty refuses rather than admits. The exclusion and the write it guards run under one per-task admission lock, so two concurrent dispatches yield one ledger record and one submit, with the loser refused before any durable write. **A Run terminal is not a Session terminal.** ARS gives Sessions no close operation and §7.4 bounds *Runs*, so the two questions have two surfaces: `observe_run()` answers about the Run and keeps every terminal honest — a `PERMISSION_VIOLATION` or reported `CONFIG_FIDELITY` Run is still `failed` there — while `status()`/`liveness()`, the surface the port drives, answer about the durable ARS Session and stay `running`. Trusted terminal truth reconciles the Run away, which both clears the §7.4 block and leaves the Session reusable for the next turn, so a conversation is not one turn long; the neutral-to-session-state projection that used to end a task at its first Run terminal is deleted rather than adjusted, since no mapping should exist. `kill()` is the one explicit task-lifecycle action and the only path to a terminal task — with no Run live it ends the task locally without reaching for a Session operation ARS does not have — and an explicitly cancelled task refuses a new dispatch rather than silently resurrecting ended work. The end-to-end composition regression proves it: two turns on one task and one ARS Session, exactly two submits, the second reusing the recorded Session verbatim, the task nonterminal throughout, and distinct turn refs with no cursor bleed. | Makes the offline contract truth match released ARS 0.7.6 before any behavioral stage builds on it. |
| ARS 0.7.7 dependency calibration | Done; superseded by the 0.7.8 calibration below | Production ARS moved 0.7.6 → 0.7.7, so the reviewed exact pin advanced in the `agent-run-supervisor` extra, its `dev` mirror, `EXPECTED_AGENT_RUN_SUPERVISOR_VERSION`, a regenerated `uv.lock`, and the narrowed `[tool.uv].exclude-newer-package` cutoff for this one package (the global 7-day supply-chain window is untouched). **The 0.7.7 calibration carries no Socket API v3 / wire / Session semantic delta:** the release change is ARS-internal (Native ACP SDK 0.12.1 transport adaptation), and the installed `agent_run_supervisor/arsd` tree is byte-identical between 0.7.6 and 0.7.7 — `api_version` / `supported_api_versions`, the closed seven-operation set, request/response shapes, Session/Run lifecycle, terminal vocabulary, and error codes are all unchanged. Nothing but the exact package-version anchors moved: the contract module, the `arsd` backend, permission behavior, result projection, `/delegate` syntax/routing, and Gateway logic are untouched. The calibration is load-bearing rather than cosmetic because `server_info` negotiation compares the daemon's reported version against the pin exactly — a stale `0.7.6` anchor would have failed the next real `/delegate` admission against the deployed 0.7.7 daemon with `runtime_arsd_version_mismatch`. The dated 0.7.6 Spec/plan pair remains the active governing integration authority and is retained unchanged as historical contract provenance. | Keeps the deployed `/delegate` integration admissible against production ARS without reopening the integration contract. |
| Semantic AGENT delegation + `/delegate` removal | Done; resident runtime active for the approved semantic-delegation surface | **The active consumed ARS baseline is `agent-run-supervisor==0.7.8`**, whose only delta is the additive read-only `agent_list` roster operation — the closed operation set is now eight, the wire stays Socket API v3, and the pin/lock/`EXPECTED_AGENT_RUN_SUPERVISOR_VERSION`/`exclude-newer-package` anchors moved together. Sachima gained an exact `agent_list` validator (canonical id grammar, strict closed shape, ascending-unique ids, bounded page) plus a backend roster read, and a new `gateway/sachima_agent_execution_presets.py` catalog binding one canonical `agent_id` to approved workspace/agent-policy/model/effort/run-limits refs and a declared permission set. Execution eligibility is exactly `live roster ∩ valid preset`: a registered AGENT with no preset reports registered-but-unavailable and never inherits a default configuration, and a preset whose id is off the roster never submits. The user-facing `/delegate` command is removed outright — registry entry, help/catalog/menu lines, Gateway cold and busy-agent routes, slash handler, selector module, mention→AGENT mapping, `resolve_route`, priority/auto-selection routing and `requested_profile_id` are all gone with no shim, deprecation reply, or migration path. The one entry point is the existing default-off `sachima_delegate_control` tool; Hermes owns understanding, clarification and correction. Three further properties hold. **Grants are sealed per policy:** an optional `grant_by_policy_ref` config map carries the exact capability set each `agent_policy_ref` submits under, a preset's declared `permissions` must equal it, and a narrowed set travels under its own deterministically derived `grant_ref`/`grant_hash`/`grant_role_hash` — so a review AGENT's Run no longer inherits the config-wide `write`, and two Runs with different authority are distinguishable afterwards. **Selection is case-insensitive exact:** preset documents still demand canonical lowercase ids, while a user/Hermes selection of `Codex` resolves to roster `codex` with no trimming, aliasing or partial matching, and the canonical roster spelling is what is admitted and stored. **Role routing is explicit and separate:** a `gateway/sachima_agent_role_policy.py` catalog holds `agent_id` → division + roles with no ranking of any kind, and a read-only `agents` control action returns the live roster annotated with executability and role facts plus, for an exact role, the single eligible candidate or a stable ambiguous/no-candidate code with the candidate list — zero or several is always a clarification, never a tie-break, and creation still names an exact `agent_id`. Durable coordinator/task/Run/Session/result behavior is unchanged and pre-rename records stay readable. | Makes natural-language Hermes delegation the only AGENT entry point, with a deterministic admission boundary underneath it. |
| Native delegation result/lifecycle projection | Done and resident runtime active | Native delegation terminal results now carry Sachima-owned semantic projection with fail-closed `summary_status`, and the accepted/running/terminal lifecycle is delivered through the approved Feishu path. The complete `dtask_*` remains the user-facing copyable task number; `dres_*`, ARS Run/Session IDs, prompts, tokens, credentials, and raw events remain internal. | Provides truthful bounded native-delegation lifecycle and result semantics without making the card projection authoritative. |
| Native delegation status card | Done as docs-only plan; source implementation not started | `docs/plans/2026-08-26-native-delegation-status-card.md` defines one Feishu card per Task, stable per-Turn round rows, same-card continuation patching, trusted Session-reuse evidence, recovery/idempotency, and compact Markdown fallback. Saving the plan does not implement or activate the card. | Next native-delegation presentation slice; separate from the existing Task Workbench TODO card. |
| S1 integration architecture/design | Done | Architecture/design task complete. | Defines Activity ↔ supervisor seam, claim-check model, failure mapping, no-leak boundary, and S2–S5 path. |
| S2 local/offline adapter seam | Done | Local/offline fake/injected seam task complete. | Provides the Activity-boundary → supervisor adapter seam with default-off admission, claim-check idempotency, no-relaunch recovery, and no-leak checks. |
| S3 Activity/controller design | Done | Activity/controller design task complete. | Defines how Temporal Activity/controller calls the S2 seam: contracts, role mapping, lifecycle, stable refs/codes, and Worker/task-queue ownership. |
| S3 hermetic-local Activity implementation | Done | Hermetic-local implementation task complete. | Adds Activity-compatible body and caller-owned controller over the S2 seam using injected-fake deterministic execution only. |
| S4 read-only real-agent step design | Done | Read-only real-agent step design task complete. | Defines how a future implementation would replace the injected fake with one bounded read-only real-agent step through agent-run-supervisor. |
| S4 read-only real-agent step implementation | Done | Bounded read-only real-agent seam implementation task complete. | Binds the S3 Activity/controller to the bounded read-only real-agent step while preserving no-leak, fail-closed, idempotency, and ops-owned lifecycle boundaries. |
| S5 downstream delivery reconnect design | Done | Downstream delivery/ACK reconnect design task complete. | Defines how the completed S4 orchestration output would reconnect to the default-off delivery/ACK surface through an injected (fake) send seam, with no real send. |
| S5 downstream delivery reconnect implementation | Done | Default-off reconnect implementation candidate complete with injected/fake send seam, S5-owned durable pre-claim, no-double-send recovery, closed mapping, no-leak, and ACK/WATCH semantics. | Binds the S4 orchestration output to the delivery/ACK controller while preserving no-leak, fail-closed, delivery idempotency, and ops-owned lifecycle boundaries. |
| P7 bounded real-send canary request-packet preparation | Done | Docs/status preparation candidate complete: prepares one bounded safe-label request packet (closed intent/channel/role/permission/target_ref/artifact_ref mapping), the S5 delivery/ACK reuse boundary, and the pre-/post-execution gates. Prepares a later execution approval; **does not authorize execution** and supplies no concrete recipient. | Downstream delivery safety support, not the current mainline. |
| P7 bounded real-send canary execute | Paused | Deliberately paused; requires a separate one-execution approval packet with concrete safe values. | Downstream delivery safety support, not the current mainline. |

## Support foundation board

| Foundation slice | Status | Why it matters now |
|---|---|---|
| P5 Temporal Slice 1 | Done | Establishes default-off, caller-owned Temporal foundation for durable orchestration. |
| P6-A controlled AI FLOW composition | Done | Provides controlled composition over the existing orchestrator and step seam using deterministic/injected execution. |
| P6-B bounded read-only real-agent step | Done for the approved bounded smoke | Proves the prerequisite bridge shape into agent-run-supervisor under strict read-only/local controls. |
| P6 runtime lifecycle / controlled attach | Done | Establishes caller-owned attach/recover boundary without starting runtime/Worker/service processes. |
| P7 delivery / ACK closure controller | Done | Provides downstream delivery safety support while staying default-off and offline. |

## Active blockers / gates

| Gate | Status | Required before |
|---|---|---|
| S5 implementation quality gate | Done | Quality closeout complete for the S5 implementation candidate; required before P7 bounded real-send canary request-packet preparation. This status page does not authorize P7 execution. |
| R3 Temporal attach-only durable bridge implementation | Done | R3 attach-only durable bridge source implementation is complete as a local/offline task; this status page does not approve Worker/service startup or live execution. |
| Agent-run-supervisor integration foundation | Done | Adapter, host-local smoke shape, persistent lifecycle, workbench view, production-shaped E2E, and live-progress safe projection are complete as local/offline/default-off foundation. The library itself is consumed only as the exact-pinned `agent-run-supervisor==0.7.8` PyPI extra — no source-checkout / `PYTHONPATH` / `sys.path` channel remains. Fresh repo/runtime checks are still required before reuse. |
| Live stream workbench/query integration | LS1-LS3 + LS4-A done; LS4 runtime/Gateway surface not approved | LS1 workbench composition, LS2 private source binding, LS3 caller-API compatibility smoke, and LS4-A default-off local/offline query activation gate are done as local/offline/default-off slices; the LS4 runtime/Gateway/Feishu query surface remains a later separately approved task. |
| Agent-run-supervisor/acpx runner pin | Done as local/offline maintenance | Sachima runner-provenance gates admit host-local overlays only when they pin verified absolute local `acpx` 0.12.0 executables. Historical 0.10.0 smoke evidence is retained as history only. This does not approve real acpx/npx/agent execution. |
| Real AGENT / ACP execution | Active only through approved semantic-delegation presets | The resident control surface may execute a canonical live ARS agent only when live registration intersects a valid execution preset and the current task is authorized. This does not authorize ad-hoc direct CLI fallback, unregistered agents, new presets, or unrelated execution surfaces. |
| Write-capable external AGENT roles | Task-scoped preset only | Write is available only when the selected role/preset and current implementation authorization require it. Destructive, credential, service-control, deployment, and unrelated workspace mutation remain separately controlled. |
| Gateway / Feishu / live / default-on behavior | Existing semantic-delegation surface active; broader activation not approved | The approved native delegation control/lifecycle/result path is resident and healthy. This does not approve the new status card, unrelated platform-adapter changes, public ingress, automatic rollout, or broader default-on behavior. |
| Production config / service lifecycle | Not approved | Production config writes, service restarts, Worker/runtime/service/subprocess startup, or production traffic. |
| P7 real-send canary execute | Paused | A real send requires a separate named approval that binds one concrete execution packet. |
| ARS Socket API v3 integration | Done and resident on ARS 0.7.8 | The accepted 0.7.6 contract and P1–P5 implementation remain historical design provenance; dependency/runtime calibration advanced through 0.7.7 to the active 0.7.8 baseline without reopening Task/Turn/Run/Session semantics. Resident ARSD 0.7.8 and the approved semantic-delegation path are active. Every execution and lifecycle action still requires its own current admission and authorization. |
| ARS effective-configuration and permission evidence | Partially proven by bounded live canaries | Live native-delegation evidence proves per-Run requested model/effort fidelity and same-ARS-Session continuation with distinct Runs plus create-then-load events. This does not generalize to every AGENT/profile/permission failure path; permission-violation ownership and any broader matrix remain separately scoped. |
| Semantic delegation runtime activation | Active for the approved resident control surface | The resident Gateway exposes `sachima_delegate_control` and resolves execution eligibility from the live ARS roster intersected with valid execution presets. Current roster, service health, config, and execution admission require fresh checks; this status grants no new config, deployment, restart, or permission authority. |
| Active-run cancellation | WATCH | Any claim that active host/ACP runs can be reliably interrupted mid-run. |

## Next allowed work

The next safe work is:

1. **Request explicit source-implementation approval for the native delegation status card** — the docs-only plan is complete; implementation stages S0–S5, deterministic tests, real Feishu/ARS canary, deployment, and Gateway restart remain separately bounded. The implementation must preserve one Task card with multiple Turn/Run round rows and keep ARS/Sachima durable state authoritative.
2. **Request or approve a separately scoped LS4 runtime/Gateway query surface** — LS1 workbench composition, LS2 private source binding, LS3 synthetic-artifact caller-API compatibility smoke, and LS4-A default-off local/offline query activation gate are done as local/offline/default-off slices; the remaining LS4 runtime/Gateway/Feishu surface is the next candidate.
3. **Keep the LS4 runtime/Gateway surface separate from live/default-on rollout** — the LS4-A gate stays local/offline (`local_offline`/`hermes_internal`) and default-off; any runtime/Gateway/query activation still needs its own named approval and must not imply real AGENT/acpx/npx launch, Gateway/Feishu/live/default-on behavior, real delivery, service lifecycle, or production config.


P7 real-send canary execution remains paused and separate from this mainline.

## Explicit non-approvals

This status page does **not** approve:

- additional R5 source/runtime work beyond the default-off local/offline canary-hardening surface except for the named `agent-run-supervisor` integration slices;
- real external Sachima ingress;
- real external delivery or production delivery control;
- P7 real-send canary execute;
- additional Gateway/Feishu/live/default-on behavior outside the approved semantic-delegation control/lifecycle/result surface;
- public webhook exposure;
- production config writes or service restarts;
- Gateway-owned Temporal/Worker/service/subprocess lifecycle;
- AGENT/ACP execution outside an authorized semantic-delegation task and valid live roster/preset intersection;
- write-capable external AGENT roles outside a task-scoped authorized implementation preset;
- Satine or Hermes-profile ACP execution;
- production cluster or production traffic;
- new or expanded AGENT execution-preset or role/division configuration writes, or broader `sachima_delegate_control` activation;
- any change to `oh-my-pi`'s existing production permission preset or approval mode;
- ungated or non-read-only roster operations against the live daemon.

## Completion semantics

A task row can move to `Done` only when the task's own scoped deliverable is complete. That does not by itself approve the next stage, live behavior, delivery, production config, or write-capable agent execution. Future stages still need their own named approvals and their own verification gates.
